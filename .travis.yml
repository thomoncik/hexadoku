language: cpp
compiler:
  - clang
script: mkdir build && cd build && cmake .. && make

notifications:
  email:
    recipients:
      - tjhomoncik@gmail.com
    on_success: change
    on_failure: always
