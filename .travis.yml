sudo: required
language: cpp

env:
    - SETUP_CURSES="sudo apt install -y libncurses5-dev libncursesw5-dev"

compiler:
    - gcc

before_install:
    - sudo apt update
    - eval "${SETUP_CURSES}"

script:
    - mkdir build && cd build
    - cmake ..
    - make
    
    
notifications:
  email:
    recipients:
      - tjhomoncik@gmail.com
    on_success: change
    on_failure: always
